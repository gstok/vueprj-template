
//按精度取一个小数
function accuracy (decimal, accu) {
    if (accu > -1) {
        let numStr = decimal.toString();
        let strSeg = numStr.split(".");
        if (strSeg.length == 1) {

        }
        else if (strSeg.length == 2) {

        }
        else {
            
        }
    }
    else {
        return decimal;
    }
}